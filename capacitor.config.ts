import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.lunarbeaverkick',
  appName: 'lunar-beaver-kick',
  webDir: 'dist'
};

export default config;
